# Next.js SSR / App Router — Netlify Essentials
# Repo yapısı: Next uygulaması `web/` altında olduğu için base burada.

[build]
  base = "web"
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
